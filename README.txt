###README

Ensure that both scripts are placed in the directory above your test_dataset

Requires a directory full of fastq files to work

All python scripts can be ran through terminal by typing "python {filename}"
Python installation can be verified by typing "python --version" into terminal

If you do not have python, it can be installed from https://www.python.org/downloads/ or sudo apt get python
If you have Windows, I'm very sorry


